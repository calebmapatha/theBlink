import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.focusblink.app',
  appName: 'FocusBlink',
  webDir: 'dist',
  android: {
    backgroundColor: '#0d0f1a',
  },
}

export default config
