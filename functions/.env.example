# Copy to functions/.env before deploying with live billing.
# Without PAYSTACK_SECRET_KEY, paid plans activate in demo mode (no charge).
PAYSTACK_SECRET_KEY=sk_live_your_key_here
# Where Paystack returns the customer after checkout.
PAYMENT_CALLBACK_URL=https://calebmapatha.github.io/theBlink/
